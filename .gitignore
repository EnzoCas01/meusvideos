node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Local VoiceStudio checkout used to generate the narration (multi-GB, not versioned)
tools/vs/

# Imagens baixadas da web (origem registrada em cada manifest.json)
public/images/
gen-comece-pequeno.log
